# Nix
result
result-*
.direnv

# Rust
target/
**/*.rs.bk
*.pdb

# npm release staging (scripts/stage-npm-packages.sh output + the README/LICENSE
# it copies into the launcher package at publish time)
/dist/
/npm/README.md
/npm/LICENSE

# OS
.DS_Store

# Environment
.env

# Local AI-tooling config
/CLAUDE.local.md
/AGENTS.local.md
/.beads

# Pre-decision design drafts, kept beside the issues they belong to rather
# than in docs/. A doc lands in docs/ when its work does — until then it is a
# plan, and it says so in its own first line ("Status: proposal").
/design

# bv (beads viewer) local config and caches
.bv/
